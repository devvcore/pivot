/** PPTX: zip of XML; MVP return placeholder. Full extraction would use pptx-parser or similar. */
export async function parsePPTX(_buffer: Buffer): Promise<string> {
  return "[PPTX slides - add pptx text extraction for full content]";
}
